{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "book_flight response",
  "type": "object",
  "required": ["booking_id", "trip_id", "flight_id", "status"],
  "properties": {
    "booking_id": {"type": "string"},
    "trip_id": {"type": "string"},
    "flight_id": {"type": "string"},
    "status": {"type": "string", "enum": ["booked", "pending", "failed"]},
    "tool_notice": {"type": "string"}
  },
  "additionalProperties": true
}
